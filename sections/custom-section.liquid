<div class="custom-section color-{{ section.settings.color_scheme }}">
    {% content_for 'blocks' %}
  </div>
  
  {% schema %}
  {
    "name": "Custom section",
    "blocks": [{ "type": "@theme" }, { "type": "@app" }],
    "presets": [
      {
        "name": "Custom section"
      },
  
    ]
  }
  {% endschema %}
  