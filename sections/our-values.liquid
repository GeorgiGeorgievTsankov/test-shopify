{{ 'our-values.section.css' |asset_url | stylesheet_tag }}
  
<div class="our-values-wrapper">
  {% for block in section.blocks %}
    {% case block.type %}
      {% when 'text' %}
        <div class="our-values-text">
          <h2>{{ block.settings.heading }}</h2>
          <p>{{ block.settings.text }}</p>
        </div>
    {% endcase %}
  {% endfor %}
  <div class="our-values-image">
    <img class="our-values" src="{{ images['our-values.png'] | image_url: width: 800 }}" alt="our-values" width="700" height="auto" />
  </div>
</div>

{% schema %}
{
  "name": "Our Values",
  "tag": "section",
  "settings": [],
  "blocks": [
    {
      "type": "text",
      "name": "Text Block",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Our Values"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "<p>Feeding your baby is a deeply personal journey, and we encourage parents to make choices that align with their family’s lifestyle. Whether you follow BLW (baby-led weaning) or traditional weaning, we support a positive, intuitive approach to food.Our mission is to empower parents and build confident eaters, ensuring that every step of the feeding journey feels supported, informed, and enjoyable.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Our-Values",
    }
  ]
}
{% endschema %}